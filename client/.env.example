# Copy to `.env.local` for local overrides (never commit `.env.local`)
# Vite only exposes variables prefixed with VITE_

# Leave empty to use the dev proxy (recommended for localhost)
VITE_API_BASE_URL=
